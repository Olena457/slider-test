import css from './Layout.module.css';

const Layout = () => {
  return (
    <div className={css.layout}>
      <main className={css.main}>Main Content</main>
    </div>
  );
};

export default Layout;
